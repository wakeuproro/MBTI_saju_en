# Pinned to verified set — unpinned starlette/anyio drift breaks JSON body parsing (400).
fastapi==0.135.3
starlette==1.0.0
anyio==4.13.0
uvicorn[standard]==0.44.0
pydantic==2.12.5
python-dotenv==1.0.1
google-generativeai==0.8.6
httpx==0.27.2
korean-lunar-calendar==0.3.1
sxtwl==2.0.6
cryptography==43.0.1
# paypal via httpx (already listed above)
# cache bust
